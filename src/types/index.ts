export type { User, Event, Zone, Spot, Reservation, Payment } from "@prisma/client";
export type { Role, EventStatus, SpotStatus, SpotType, ReservationMode, ReservationStatus, PaymentStatus } from "@prisma/client";
